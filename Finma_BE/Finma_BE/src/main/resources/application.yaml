server:
  port: 8080
  servlet:
    context-path: /finma

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/finance_app?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

  mail:
    host: smtp.gmail.com
    port: 587
    username: ngoductuancuong@gmail.com
    password: vuzw qpwn nmmu ohmb
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET}
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
            scope:
              - openid
              - profile
              - email
          facebook:
            client-id: ${FACEBOOK_CLIENT_ID}
            client-secret: ${FACEBOOK_CLIENT_SECRET}
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
            scope:
              - email
              - public_profile

jwt:
  signing-key: "bH0IkTZmDe64duaEqMr8jjDhg8naOaBuszFAdowreJE="
  valid-Duration: 1   # 1 hour
  refresh-Token-Duration: 24 # 24 hours